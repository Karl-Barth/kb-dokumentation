# `<msDesc>`

contains a description of a single identifiable
    manuscript or other text-bearing object such as an early printed book.

**Modul:** msdescription

## Content-Model

`<msIdentifier>`, model.headLike, model.pLike, `<msContents>`, `<physDesc>`, `<history>`, `<additional>`, `<msPart>`, `<msFrag>`

## Constraints

- **one_ms_singleton_max**: Only one  is allowed as a child of .
